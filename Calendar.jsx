import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Calendar() {
  return (
    <Box p={3}>
      <Typography variant="h4">Calendar</Typography>
      <Typography>This is the calendar page.</Typography>
    </Box>
  );
}