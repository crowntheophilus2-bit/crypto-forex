import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Trade() {
  return (
    <Box p={3}>
      <Typography variant="h4">Trade</Typography>
      <Typography>This is the trade page.</Typography>
    </Box>
  );
}