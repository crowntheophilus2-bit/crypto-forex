import React from 'react';
import { Typography, Box } from '@mui/material';

export default function SubscribeSignals() {
  return (
    <Box p={3}>
      <Typography variant="h4">Subscribe Signals</Typography>
      <Typography>This is the subscribe signals page.</Typography>
    </Box>
  );
}