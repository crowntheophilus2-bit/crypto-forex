import React from 'react';
import { Typography, Box } from '@mui/material';

export default function TradeHistory() {
  return (
    <Box p={3}>
      <Typography variant="h4">Trade History</Typography>
      <Typography>This is the trade history page.</Typography>
    </Box>
  );
}