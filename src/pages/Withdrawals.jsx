import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Withdrawals() {
  return (
    <Box p={3}>
      <Typography variant="h4">Withdrawals</Typography>
      <Typography>This is the withdrawals page.</Typography>
    </Box>
  );
}