import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Chart() {
  return (
    <Box p={3}>
      <Typography variant="h4">Chart</Typography>
      <Typography>This is the chart page.</Typography>
    </Box>
  );
}