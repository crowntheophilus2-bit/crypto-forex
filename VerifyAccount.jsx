import React from 'react';
import { Typography, Box } from '@mui/material';

export default function VerifyAccount() {
  return (
    <Box p={3}>
      <Typography variant="h4">Verify Account</Typography>
      <Typography>This is the verify account page.</Typography>
    </Box>
  );
}